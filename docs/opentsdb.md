# OpenTSDB

```coffeescript
{time_series, start, end, aggregation, group}
```

## `time_series`

```coffeescript
{aggregation, metric_name, downsample, tags, rate}
```

## `downsample`

```coffeescript
{period, aggregation}
```
